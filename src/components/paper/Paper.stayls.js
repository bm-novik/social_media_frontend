import {styled} from "@mui/material/styles";
import {Paper} from "@mui/material";

export const PostDetailCommentPaper = styled(Paper)(({theme}) => ({
    maxWidth: '62.5ch',
    minWidth: '50ch',
    backgroundColor: 'white',
    paddingBottom: 0,
    paddingTop: 0,
    height: "100%",
    width: '100%',
    borderTopLeftRadius: 0,
    borderBottomLeftRadius: 0,

}))
